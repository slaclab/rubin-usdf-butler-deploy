resources:
- ns.yaml
  ## cnpg
  #- database.yaml
  #- db-svc.yaml
  #- db-svc-to-remove.yaml
  #- pooler-tx-rw-svc.yaml
  #- pooler-tx-rw-svc-to-remove.yaml
  #- pooler-ss-rw-svc.yaml
  #- pooler-ss-rw-svc-to-remove.yaml

  #secretGenerator:
  #- name: usdf-butler-embargo-creds
  #  options:
  #    disableNameSuffixHash: true
  #  files:
  #  - etc/.secrets/username
  #  - etc/.secrets/password

- name: s3-creds
  options:
    disableNameSuffixHash: true
  files:
  - ACCESS_KEY_ID=etc/.secrets/client-id
  - ACCESS_SECRET_KEY=etc/.secrets/client-secret

namespace: embargo-dev
