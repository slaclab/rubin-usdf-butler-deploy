resources:
- ns.yaml
# cnpg
- database.yaml
- npg-butler-embargo-pooler.yaml

secretGenerator:
- name: usdf-butler-embargo-creds
  options:
    disableNameSuffixHash: true
  files:
  - etc/.secrets/username
  - etc/.secrets/password

namespace: embargo-dev
